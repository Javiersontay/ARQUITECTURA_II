{
  "name": "assignment-05",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "prisma": "^7.5.0",
    "turbo": "^2.5.0"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0"
  }
}
